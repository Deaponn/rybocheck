import 'package:flutter/material.dart';

class MapPost extends StatelessWidget{
  const MapPost({super.key});

  @override
  Widget build(BuildContext context){
    return const Text("ShowMapPost page", textAlign: TextAlign.center);
  }
}
