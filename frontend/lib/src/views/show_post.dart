import 'package:flutter/material.dart';

class ShowPost extends StatelessWidget{
  const ShowPost({super.key});

  @override
  Widget build(BuildContext context){
    return const Text("ShowPost page", textAlign: TextAlign.center);
  }
}
